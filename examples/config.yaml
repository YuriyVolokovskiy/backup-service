s3:
  endpoint: "${S3_ENDPOINT}"
  region: "${S3_REGION}"
  bucket: "${S3_BUCKET}"
  access_key_id: "${S3_ACCESS_KEY_ID}"
  secret_access_key: "${S3_SECRET_ACCESS_KEY}"
  force_path_style: true

telegram:
  enabled: true
  bot_token: "${TELEGRAM_BOT_TOKEN}"
  chat_id: "${TELEGRAM_CHAT_ID}"
  success_enabled: true

defaults:
  retention:
    daily: 7
    weekly: 4
    monthly: 12
  backup_cron: "0 2 * * *"
  cleanup_cron: "0 5 * * *"
  compression_level: 9

targets:
  - id: example-app-prod
    engine: postgres
    database_url_env: EXAMPLE_APP_PROD_DATABASE_URL
    s3_prefix: example-app/prod/postgres
    backup_cron: "0 2 * * *"
    cleanup_cron: "0 5 * * *"
    retention:
      daily: 7
      weekly: 4
      monthly: 12
